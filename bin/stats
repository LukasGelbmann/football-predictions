#!/bin/sh --

PROJECT_PATH=$(dirname -- "$(dirname -- "$(realpath -- "$0")")")
SCRIPT_PATH="${PROJECT_PATH}/src/stats.py"

if type pypy3 >/dev/null 2>&1; then
    pypy3 -- "${SCRIPT_PATH}" "$@"
else
    python3 -- "${SCRIPT_PATH}" "$@"
fi
